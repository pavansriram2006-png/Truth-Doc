services:
  - type: web
    name: truthdoc-ocr-backend
    runtime: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: CORS_ALLOWED_ORIGINS
        value: https://truth-doc.vercel.app
